{
  "name": "foursight",
  "version": "1.0.0",
  "description": "FourSight (DORA Tracker for GitHub)",
  "scripts": {
    "build": "rimraf ./package/js && rimraf package.zip && npx webpack && cd package && zip -r ../package.zip .",
    "dev": "webpack --mode development --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "rimraf": "^5.0.5",
    "sharp": "^0.34.5",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  }
}
